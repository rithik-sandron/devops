apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
        - name: hello
          image: rithiksandron/app:1

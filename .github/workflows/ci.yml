name: Update Major Release Tag

on:
  push:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/composite/action.yml
        id: ci
